remove the printing of the thing in build.toml runs
fix init project bug where it inits anyway even if you have a project, all it does is overwrite build.toml and config.toml
path does not work when python is on path // what does this mean???
item on path, does not work if its added by the user and also in a subdirectoy?
the path adding does not work properly 